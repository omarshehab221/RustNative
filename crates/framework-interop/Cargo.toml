[package]
name = "framework-interop"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Library-only mode for Rust Native: one interface description (.ril), from which C, C#, and Rust bindings are generated, and the runtime the generated shims call"
repository.workspace = true
homepage.workspace = true
readme.workspace = true
keywords = ["ffi", "bindings", "interop", "idl", "framework"]
categories = ["development-tools::ffi"]

[lints]
workspace = true

[dependencies]

[dev-dependencies]
cc = "1"
